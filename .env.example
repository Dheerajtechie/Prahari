# PRAHARI — Copy to .env and fill in. Never commit .env to git.

# Required for launch
PORT=3001
# Supabase example (project vqrmqgbmqlhqnjlalokd): get URI from Project Settings → Database
# DATABASE_URL=postgresql://postgres.vqrmqgbmqlhqnjlalokd:YOUR_PASSWORD@aws-0-REGION.pooler.supabase.com:6543/postgres?sslmode=require
DATABASE_URL=postgresql://user:password@host:5432/prahari
JWT_SECRET=generate_with_openssl_rand_hex_64

# When frontend is on a different domain (e.g. Netlify)
FRONTEND_URL=https://prahari.in

# Optional: for production CORS (comma-separated if multiple)
# NODE_ENV=production
